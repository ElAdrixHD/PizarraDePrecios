<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="es.adrianmmudarra.pizarradeprecios">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.producto.ProductoActivity"></activity>
        <activity android:name=".ui.subasta.SubastaActivity" />
        <activity android:name=".ui.cooperativa.CooperativaActivity" />
        <activity android:name=".ui.dash.DashActivity" />
        <activity
            android:name=".ui.inicio.RegistroActivity"
            android:theme="@style/AppThemeNoBar" />
        <activity
            android:name=".ui.inicio.LoginActivity"
            android:theme="@style/AppThemeNoBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>